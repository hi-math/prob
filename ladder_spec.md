# 12 — 사다리타기 시뮬레이션 수정 스펙

기존 `app/sim/12-ladder/page.tsx` 를 아래 스펙으로 전면 교체한다.

---

## 전체 흐름 (3단계)

```
[STEP 1] 설정     →     [STEP 2] 선택     →     [STEP 3] 결과
사다리 수 입력          사다리 하나 클릭          집계 차트 표시
횟수 입력               선택 확정 버튼            다시하기 버튼
시작 버튼
```

각 단계는 같은 페이지에서 `step` state로 전환한다. 뒤로가기 버튼으로 이전 단계 복귀 가능.

---

## STEP 1 — 설정

### UI 구성

- 숫자 입력 2개 + 시작 버튼

```
사다리 수      [  4  ] (2 ~ 8)
시뮬레이션 횟수 [ 100 ] (10 ~ 10000)

            [ 시작하기 ]
```

### 입력 검증

| 항목 | 최솟값 | 최댓값 | 기본값 |
|------|--------|--------|--------|
| 사다리 수 | 2 | 8 | 4 |
| 횟수 | 10 | 10000 | 100 |

- 범위 벗어나면 인라인 에러 메시지 표시
- 시작 버튼 클릭 시 STEP 2로 전환

---

## STEP 2 — 사다리 선택

### UI 구성

```
┌─────────────────────────────────┐
│  어느 사다리를 선택하시겠어요?     │
│  (사다리를 클릭하세요)            │
├─────────────────────────────────┤
│                                 │
│   Canvas — 사다리 그림           │
│   (고정된 사다리 1개 표시)        │
│                                 │
├─────────────────────────────────┤
│  선택된 사다리: 없음              │
│          [ 선택 확정 ]  ← 선택 후 활성화
└─────────────────────────────────┘
```

### Canvas 사다리 그리기

**구조:**
- 세로 기둥: 사다리 수만큼 균등 배치
- 가로 다리: 랜덤 생성 (인접한 두 기둥 사이만 연결)
  - 다리 밀도: 기둥 간격당 평균 2~3개
  - 같은 높이에 겹치는 다리 없음 (인접 기둥 사이 충돌 방지)
- 상단: 출발 번호 표시 (1, 2, 3 ... N)
- 하단: 도착 번호 표시 (1, 2, 3 ... N, 랜덤 배치)

**인터랙션:**
- 상단 기둥 클릭 → 해당 사다리 강조 (색상: `#534AB7`, 두께 3px)
- 다른 기둥 클릭 → 선택 변경
- 선택된 사다리 위에 ▼ 아이콘 표시

**이 단계에서 경로는 표시하지 않는다.**

---

## STEP 3 — 시뮬레이션 결과

### 시뮬레이션 로직

```
입력한 횟수만큼 반복:
  1. 사다리 다리를 새로 랜덤 생성
  2. 사용자가 선택한 번호에서 출발
  3. 사다리 규칙대로 내려감
     - 현재 위치에 가로 다리가 있으면 건너감
     - 끝까지 내려가서 도착 번호 기록
  4. 도착 번호별 횟수 집계
```

### UI 구성

```
┌──────────────────────────────────────┐
│  선택한 사다리: 2번                    │
│  시뮬레이션: 100회                    │
├───────────────────┬──────────────────┤
│  Canvas           │  결과 요약        │
│  (마지막 시뮬레이션 │                  │
│   사다리 + 경로)   │  가장 많이 나온 곳│
│                   │  ▶ 3번 (34회)    │
│                   │                  │
│                   │  도착지별 확률    │
│                   │  1번 ██░░ 18%    │
│                   │  2번 ███░ 24%    │
│                   │  3번 ████ 34%    │
│                   │  4번 ██░░ 24%    │
├───────────────────┴──────────────────┤
│  도수분포 BarChart (Recharts)          │
│  x축: 도착 번호 / y축: 횟수            │
│  이론값(1/N) 점선 오버레이             │
└──────────────────────────────────────┘
│  [ 다시하기 ]   [ 설정으로 ]           │
└──────────────────────────────────────┘
```

### Canvas (결과 화면)

- 마지막으로 실행된 시뮬레이션의 사다리를 표시
- 선택한 경로를 빨간색(`#E24B4A`)으로 애니메이션 없이 표시
- 도착 지점 강조 (채움 원)

### BarChart

- Recharts `BarChart` 사용
- x축: 도착 번호 (1 ~ N)
- y축: 횟수
- 이론값 `100/N` 회를 회색 점선 `ReferenceLine`으로 표시
- 가장 많이 나온 막대만 `#534AB7`, 나머지 `#EEEDFE`

---

## 상태 관리

```ts
type Step = 'setup' | 'select' | 'result'

// STEP 1
ladderCount: number        // 사다리 수
simCount: number           // 시뮬레이션 횟수

// STEP 2
fixedLadder: Bridge[]      // 선택 화면용 고정 사다리 구조
selectedIndex: number | null  // 선택한 사다리 번호 (0-based)

// STEP 3
results: number[]          // 각 시뮬레이션의 도착 번호 배열
lastLadder: Bridge[]       // 마지막 시뮬레이션 사다리 구조
lastPath: number[]         // 마지막 경로 (y좌표 기준 노드 배열)
tally: Record<number, number>  // 도착 번호별 집계
```

### Bridge 타입

```ts
type Bridge = {
  row: number    // 세로 위치 (0 ~ rows-1)
  col: number    // 왼쪽 기둥 번호 (0-based, col과 col+1 사이)
}
```

---

## 사다리 생성 함수

```ts
function generateLadder(n: number): Bridge[] {
  // n: 사다리(기둥) 수
  // 세로를 12칸으로 나눔
  // 각 row에서 겹치지 않게 랜덤으로 다리 배치
  // 반환: Bridge[] (다리 목록)
}
```

규칙:
- 같은 row에 인접한 다리 금지 (col i와 col i+1이 있으면 col i+1과 col i+2는 같은 row 불가)
- 전체 다리 수: `n * 2` ~ `n * 3` 개 사이

---

## 사다리 이동 함수

```ts
function tracePath(
  bridges: Bridge[],
  startCol: number,
  totalRows: number
): number[] {
  // startCol에서 출발해서 내려가며 경로 추적
  // 각 row마다 현재 col에 연결된 다리 있으면 이동
  // 반환: 거쳐간 col 배열 (길이 = totalRows + 1)
  // 마지막 값이 도착 번호
}
```

---

## 디자인 토큰

```
--accent:       #534AB7
--accent-light: #EEEDFE
--surface:      #f8f8f6
--danger:       #E24B4A
--border:       rgba(0,0,0,0.1)
--text:         #111
--muted:        #666
```

---

## 파일 위치

```
app/sim/12-ladder/page.tsx   ← 단일 파일로 전부 구현
```

외부 라이브러리: `recharts` 만 사용. Canvas 조작은 vanilla JS (`useRef`).
