# 13 — 개미 최단경로 (ACO) 시뮬레이션

## 개요

개미 군집 최적화(Ant Colony Optimization) 알고리즘을 시각화하는 인터랙티브 시뮬레이션.  
`app/sim/13-ant/page.tsx` 에 구현한다.  
기존 프로젝트의 `SimLayout`, `lib/rng.ts` 를 그대로 사용한다.

---

## 레이아웃

```
┌─────────────────────────────────────────────┐
│  ← 목차로                                    │
│  개미 최단경로 (ACO)                          │
│  페로몬 기반 개미 군집이 최단경로를 찾아갑니다  │
├──────────────────┬──────────────────────────┤
│   Canvas (왼쪽)  │   컨트롤 패널 (오른쪽)     │
│   480 × 480      │   슬라이더 + 버튼          │
└──────────────────┴──────────────────────────┘
│  세대별 최단거리 LineChart (하단 전체 폭)      │
└─────────────────────────────────────────────┘
```

---

## Canvas 시각화

### 노드
- 6개 고정 노드, 위치는 앱 초기화 시 랜덤 생성 (여백 60px 확보)
- 원 반지름 14px, 배경 흰색, 테두리 `#534AB7`
- 노드 번호 중앙 표시 (0번 = 시작/끝 노드, 강조색)
- 시작 노드(0번)는 채움색 `#534AB7`, 텍스트 흰색

### 엣지
- 모든 노드 쌍을 완전 그래프로 연결
- 기본 선 색상: `rgba(0,0,0,0.08)`, 두께 1px
- 페로몬 농도에 따라 선 두께(1~6px)와 색상 불투명도(0.1~0.8) 변화
  - 페로몬이 강한 엣지일수록 `#534AB7` 계열로 진하게 표시

### 개미 애니메이션
- 개미는 작은 원(반지름 4px)으로 표현, 색상 `#E24B4A`
- 매 세대마다 설정된 개미 수만큼 동시에 출발
- 각 개미는 현재 노드에서 다음 노드로 직선 이동 (보간 애니메이션)
- 이동 속도: 슬라이더로 조절

### 최단경로 표시
- 현재 세대 기준 최단경로 엣지를 `#E24B4A`, 두께 3px로 강조
- 경로 위에 총 거리 텍스트 표시

---

## 컨트롤 패널

### 슬라이더 4개

| 슬라이더 | 범위 | 기본값 | 설명 |
|---------|------|--------|------|
| 개미 수 | 5 ~ 30 | 10 | 세대당 개미 수 |
| 페로몬 증발률 (ρ) | 0.1 ~ 0.9 | 0.5 | 높을수록 빠르게 증발 |
| 탐험 vs 착취 (α/β) | 1 ~ 5 | 2 | 높을수록 페로몬 중시 |
| 애니메이션 속도 | 1 ~ 5 | 3 | 세대 진행 속도 |

### 버튼

- **시작** — 시뮬레이션 실행 / 정지 토글
- **1세대** — 한 세대만 실행 (정지 상태일 때만 활성)
- **노드 재생성** — 노드 위치 새로 랜덤 생성 후 초기화
- **초기화** — 페로몬·기록 리셋, 노드 위치 유지

### 상태 카드 (컨트롤 패널 하단)

| 카드 | 내용 |
|------|------|
| 세대 | 현재 세대 수 |
| 현재 최단거리 | 소수점 1자리 |
| 이론 최단거리 | — (표시 안 함, 아래 참고) |

> **핵심**: 이론적 최단거리(브루트포스 정답)는 계산하되 UI에 표시하지 않는다.  
> 시뮬레이션 종료 후 "정답 확인" 버튼을 누를 때만 공개한다.

---

## 정답 공개 흐름

```
시뮬레이션 실행 중 / 종료
        ↓
하단에 "정답 확인" 버튼 표시 (항상 보임)
        ↓
버튼 클릭
        ↓
이론 최단거리 공개 + ACO 결과와 비교
"ACO가 찾은 경로: 182.3  |  최적 경로: 178.1  |  오차: 2.4%"
        ↓
"다시하기" 버튼 표시
```

- 정답 확인 전: 버튼 텍스트 "정답 확인 🔒"
- 정답 확인 후: 버튼 텍스트 "정답 확인 🔓" (비활성화)
- 최적 경로 엣지를 초록색(`#22C55E`)으로 오버레이

---

## 하단 LineChart

- Recharts `LineChart` 사용
- x축: 세대 번호
- y축: 거리
- 라인 1 (빨강 실선): 세대별 최단거리
- 라인 2 (회색 점선, 옅게): 세대별 평균거리
- 정답 확인 후에만 초록 수평 점선(최적해) 추가

---

## ACO 알고리즘 구현

### 데이터 구조

```ts
// 노드
type Node = { id: number; x: number; y: number }

// 페로몬 행렬 (대칭)
// pheromone[i][j] = 노드 i → j 페로몬 농도, 초기값 1.0

// 거리 행렬
// dist[i][j] = 유클리드 거리
```

### 전이 확률

```
P(i→j) = [τ(i,j)^α × η(i,j)^β] / Σ[τ(i,k)^α × η(i,k)^β]

τ(i,j) : 페로몬 농도
η(i,j) : 1 / dist[i][j]  (휴리스틱)
α      : 페로몬 중요도 (슬라이더)
β      : 휴리스틱 중요도 = 5 - α + 1 (α와 반비례)
```

### 세대 진행

```
1. 모든 개미를 노드 0에서 출발
2. 각 개미: 미방문 노드 중 전이 확률로 다음 노드 선택
3. 모든 노드 방문 후 노드 0으로 귀환 → 경로 완성
4. 페로몬 증발: τ(i,j) ← τ(i,j) × (1 - ρ)
5. 페로몬 적립: 각 개미의 경로에 Q/L 추가 (Q=100, L=경로 총 길이)
6. 이번 세대 최단경로 기록
```

### 이론 최단거리 계산

- 노드 6개이므로 브루트포스(5! = 120가지) 로 정확한 최적해 계산
- 앱 초기화 및 노드 재생성 시 백그라운드에서 계산
- 결과는 state에 저장하되 UI에 노출하지 않음

---

## 상태 관리 (useState)

```ts
nodes: Node[]                  // 노드 위치
pheromone: number[][]          // 페로몬 행렬
dist: number[][]               // 거리 행렬
generation: number             // 현재 세대
bestPath: number[]             // 현재까지 최단 경로 (노드 인덱스 배열)
bestDist: number               // 현재까지 최단 거리
history: { best: number; avg: number }[]  // 세대별 기록
optimalDist: number            // 브루트포스 최적해 (숨김)
optimalPath: number[]          // 브루트포스 최적 경로 (숨김)
revealed: boolean              // 정답 공개 여부
running: boolean               // 실행 중 여부
// 슬라이더 값들...
```

---

## 애니메이션 구조

- `useRef`로 canvas 접근
- `requestAnimationFrame` 루프로 개미 이동 보간
- 세대 완료 후 페로몬 업데이트 → 다음 세대 자동 진행
- 속도 슬라이더: 낮으면 개미 이동을 천천히, 높으면 세대를 빠르게 스킵

---

## 디자인 토큰

```
--accent:       #534AB7
--accent-light: #EEEDFE
--surface:      #f8f8f6
--danger:       #E24B4A
--success:      #22C55E
--text:         #111
--muted:        #666
--border:       rgba(0,0,0,0.1)
```

---

## 파일 위치

```
app/sim/13-ant/page.tsx   ← 여기에 전부 구현 (단일 파일)
```

외부 라이브러리는 `recharts` 만 사용. canvas 조작은 vanilla JS.
